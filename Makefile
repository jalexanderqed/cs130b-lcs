# THIS DOESN'T ACTUALLY DO ANYTHING
# All compilation is done by the shell wrapper file.
# I wasn't sure if you still wanted the clean target to work correctly for Java.

all:

clean:
	@rm -rf *.class
